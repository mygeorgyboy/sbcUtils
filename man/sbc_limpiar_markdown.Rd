% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sbc_limpiar_markdown}
\alias{sbc_limpiar_markdown}
\title{Limpiar formato Markdown de textos de encuestas}
\usage{
sbc_limpiar_markdown(texto)

sbc_limpiar_markdown(texto)

sbc_limpiar_markdown(texto)
}
\arguments{
\item{texto}{Vector de caracteres con formato Markdown}

\item{preservar_saltos}{Lógico. Si TRUE, mantiene algunos saltos de línea
en lugar de convertirlos todos a espacios. Por defecto FALSE.}
}
\value{
Vector de caracteres con texto limpio en formato plano

Vector de caracteres con texto limpio en formato plano

Vector de caracteres con texto plano limpio
}
\description{
Convierte texto con formato Markdown a texto plano, manejando de forma
inteligente elementos comunes como negritas, cursivas y listas. La función
está diseñada para procesar respuestas de encuestas que contienen formato
Markdown del software de captura.

Convierte texto con formato Markdown a texto plano, manejando inteligentemente
diferentes elementos de formato como negrita, cursiva, listas y saltos de línea.
Especialmente útil para limpiar respuestas de encuestas que vienen formateadas
en Markdown.

Convierte texto con formato Markdown a texto plano de forma inteligente,
preservando contenido relevante y transformando listas en formato legible.
}
\details{
La función aplica las siguientes transformaciones:
\subsection{Formato de texto:}{
\itemize{
\item \strong{Bold}: \verb{**texto**} → \code{texto}
\item \emph{Italic}: \verb{*texto*} → \code{texto} (solo si está balanceado)
}
}

\subsection{Listas:}{
\itemize{
\item Convierte listas con título en formato \verb{título: item1, item2, item3}
\item Ejemplo: \verb{**Título**\\n- Item1\\n- Item2} → \verb{Título: Item1, Item2}
}
}

\subsection{Preservación inteligente:}{
\itemize{
\item Guiones en palabras como "Coca-Cola" se mantienen
\item Asteriscos sueltos como "Marca *" se mantienen
\item Solo elimina formato Markdown válido
}
}

La función procesa los siguientes elementos de Markdown:
\subsection{Formato de texto:}{
\itemize{
\item \strong{Negrita}: \verb{**texto**} se convierte en \code{texto}
\item \emph{Cursiva}: \verb{*texto*} se convierte en \code{texto} (solo si está balanceado)
}
}

\subsection{Listas:}{

Convierte listas con formato:

\if{html}{\out{<div class="sourceCode">}}\preformatted{**Título**
- Elemento 1
- Elemento 2
}\if{html}{\out{</div>}}

En: \verb{Título: Elemento 1, Elemento 2}
}

\subsection{Preservación inteligente:}{
\itemize{
\item Mantiene guiones en palabras: \code{Coca-Cola} permanece intacto
\item Mantiene asteriscos sueltos: \verb{Marca *} permanece intacto
\item Solo procesa listas que siguen el patrón estándar de Markdown
}
}

La función maneja los siguientes elementos de Markdown:
\subsection{Elementos que elimina:}{
\itemize{
\item \strong{Texto en negrita}: \verb{**texto**} → \code{texto}
\item \emph{Texto en cursiva}: \verb{*texto*} → \code{texto} (solo si está balanceado)
\item Guiones de lista al inicio de línea
\item Saltos de línea múltiples
}
}

\subsection{Elementos que preserva:}{
\itemize{
\item Guiones dentro de palabras: \code{Coca-Cola} (sin cambios)
\item Asteriscos sueltos: \verb{Precio: $50*} (sin cambios)
\item Asteriscos no balanceados: \code{3*4=12} (sin cambios)
}
}

\subsection{Transformación de listas:}{

Las listas se convierten al formato "título: item1, item2, item3"
}
}
\examples{
# Texto con formato bold
sbc_limpiar_markdown("**Producto Principal**")
#> "Producto Principal"

# Lista con título
texto_lista <- "**Categorías:**\n- Bebidas\n- Snacks\n- Lácteos"
sbc_limpiar_markdown(texto_lista)
#> "Categorías: Bebidas, Snacks, Lácteos"

# Texto con guiones preservados
sbc_limpiar_markdown("Coca-Cola es *refrescante*")
#> "Coca-Cola es refrescante"

# Aplicar a columna de data.frame
\dontrun{
datos <- datos |>
  mutate(respuesta_limpia = sbc_limpiar_markdown(respuesta_original))
}

# Texto con negrita
sbc_limpiar_markdown("**Texto importante**")
# Resultado: "Texto importante"

# Lista con título
texto_lista <- "**Opciones disponibles**\n- Opción A\n- Opción B\n- Opción C"
sbc_limpiar_markdown(texto_lista)
# Resultado: "Opciones disponibles: Opción A, Opción B, Opción C"

# Texto mixto
texto_mixto <- "**Marcas**: *Coca-Cola*, Pepsi-Cola"
sbc_limpiar_markdown(texto_mixto)
# Resultado: "Marcas: Coca-Cola, Pepsi-Cola"

# Vectorizado
textos <- c("**Bold**", "*Italic*", "Normal text")
sbc_limpiar_markdown(textos)

# Texto con formato bold
sbc_limpiar_markdown("**Importante:** Este es el texto")
# → "Importante: Este es el texto"

# Lista con título
texto_lista <- "**Opciones disponibles:**\n- Opción A\n- Opción B\n- Opción C"
sbc_limpiar_markdown(texto_lista)
# → "Opciones disponibles: Opción A, Opción B, Opción C"

# Preserva guiones en palabras
sbc_limpiar_markdown("Coca-Cola es una marca")
# → "Coca-Cola es una marca"

# Múltiples textos
textos <- c("**Bold**", "*Italic*", "Normal text")
sbc_limpiar_markdown(textos)
# → c("Bold", "Italic", "Normal text")

}
\concept{text_processing}

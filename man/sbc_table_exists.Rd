% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db.R
\name{sbc_table_exists}
\alias{sbc_table_exists}
\title{Verificar si una tabla existe en la base de datos}
\usage{
sbc_table_exists(conn, nombre_tabla, ignorar_caso = TRUE)
}
\arguments{
\item{conn}{Conexión DBI válida}

\item{nombre_tabla}{Nombre de la tabla a verificar (sensible a mayúsculas/minúsculas)}

\item{ignorar_caso}{Ignorar diferencias entre mayúsculas y minúsculas (default: TRUE)}
}
\value{
\code{TRUE} si la tabla existe, \code{FALSE} en caso contrario
}
\description{
Comprueba si una tabla específica existe en la base de datos conectada.
}
\examples{
\dontrun{
conn <- sbc_connect(config)

# Verificar si existe una tabla
if (sbc_table_exists(conn, "usuarios")) {
  datos <- sbc_query(conn, "SELECT * FROM usuarios")
}

sbc_disconnect(conn)
}
}
